{
  "name": "types2graphql",
  "version": "0.2.0",
  "description": "A GraphQL Schema Generator (Generates from typescript interfaces)",
  "main": "dist/src/types2graphql.js",
  "types": "dist/src/types2graphql.d.ts",
  "files": [
    "/dist/**/*"
  ],
  "scripts": {
    "build": "tsc"
  },
  "author": "Double-DOS",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.9.0",
    "typescript": "^5.2.2"
  }
}
